[project]
name = "voice-agent-backend"
version = "0.1.0"
description = "FastAPI backend bridging Google ADK voice agent over WebSocket"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "google-adk>=1.16.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]

[tool.uv.sources]



